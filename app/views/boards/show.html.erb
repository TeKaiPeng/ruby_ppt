<h1  data-controller="heart" data-heart-board ="<%= @board.id %>">
    看板 <%= @board.title %> 
    <%= link_to "我的最愛", favorites_path %>

    <button data-action="click->heart#favorite">
        <% if @board.favorited_by?(current_user) %>
            <i class="fas fa-heart" data-target="heart.heart"></i>
        <% else %>
            <i class="far fa-heart" data-target="heart.heart"></i>
        <% end %>
    </button>
</h1>


<% params[:id]%>
    <div>
        [版主：<%= display_bm(@board) %>]
    </div>
<%= link_to '新增文章', new_board_post_path(@board) %>


<ul>
    <% @post.each do |post| %>
        <li>
            <%= post.display_username %> 

            <%= link_to post.title, post_path(post) %>
            <%= link_to "編輯", edit_post_path(post) %>
            <%= link_to "刪除", post_path(post), method:'delete', data: {confirm: '確認刪除？'}%>
        </li>
    <% end %>
</ul>

<%= link_to 'back to last page', boards_path %>